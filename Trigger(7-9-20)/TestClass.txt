@isTest
public class TaskTest {
    @isTest static void testTaskUpdateDelete(){
        Account a = new Account(Name = 'AC 1');
        insert a;
        
        Task t = new Task();
        t.Subject = 'Call';
        t.Status = 'Not Started';
        t.Priority = 'Normal';
        t.WhatId = a.id;
        insert t;
        
        Task tt = new Task();
        tt.Subject = 'Call';
        tt.Status = 'Completed';
        tt.Priority = 'Normal';
        tt.WhatId = a.id;
        insert tt;
        
        
        Test.startTest();
        Task t1 = [select Status from task where id=:t.Id];
        t1.Status = 'Completed';
        update t1;
        delete tt;
        Test.stopTest();
        
    }
}